<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:converters="clr-namespace:Integreat.Shared.Converters;assembly=Integreat.Droid"
             x:Class="Integreat.Shared.Pages.PageSearchList" Title="Search">
  <ContentPage.Resources>
    <ResourceDictionary>
      <converters:UriImageSourceConverter x:Key="ImageConverter" />
    </ResourceDictionary>
  </ContentPage.Resources>
  <ContentPage.Content>
    <StackLayout>
      <Entry x:Name="SearchEntry" Placeholder="Search for" Text="{Binding SearchText}" />
      <ListView x:Name="listView" ItemsSource="{Binding FoundPages}">
        <ListView.ItemTemplate>
          <DataTemplate>
            <ImageCell
            TextColor="#FFA000"
            Text="{Binding Title}"
            Detail="{Binding Description}"
            ImageSource="{Binding Thumbnail, Converter={StaticResource ImageConverter}}">
            </ImageCell>
          </DataTemplate>
        </ListView.ItemTemplate>
      </ListView>
    </StackLayout>
  </ContentPage.Content>
</ContentPage>