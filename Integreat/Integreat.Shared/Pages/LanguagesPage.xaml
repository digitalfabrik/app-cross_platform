<?xml version="1.0" encoding="utf-8" ?>
<pages:BaseContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Integreat.Shared.Pages.LanguagesPage"
             xmlns:controls="clr-namespace:DLToolkit.Forms.Controls;assembly=DLToolkit.Forms.Controls.FlowListView"
             xmlns:pages="clr-namespace:Integreat.Shared.Pages"
             BackgroundColor="#FFC107"
             Title="" Icon="{Binding Icon}"  IsBusy="{Binding IsBusy}">
  <ContentPage.Resources>
    <ResourceDictionary>
      <Style x:Key="TitleStyle" TargetType="Label">
        <Setter Property="TextColor" Value="White" />
        <Setter Property="FontAttributes" Value="Bold" />
        <Setter Property="FontSize" Value="24"/>
      </Style>
      <Style x:Key="DescriptionStyle" TargetType="Label">
        <Setter Property="TextColor" Value="White" />
        <Setter Property="FontSize" Value="18"/>
      </Style>
    </ResourceDictionary>
  </ContentPage.Resources>
  <ContentPage.Content>
    <StackLayout Orientation="Vertical">
        <Label Text="{Binding Title}" VerticalOptions="Center" HorizontalOptions="Center" Style="{StaticResource TitleStyle}" />
        <Label Text="{Binding Description}" VerticalOptions="Center" HorizontalOptions="Center" Style="{StaticResource DescriptionStyle}" />
        <controls:FlowListView HorizontalOptions="FillAndExpand" VerticalOptions="FillAndExpand"
              IsPullToRefreshEnabled="True"
              RefreshCommand="{Binding ForceRefreshLanguagesCommand}"
              IsRefreshing="{Binding IsBusy, Mode=OneWay}"
			FlowItemsSource="{Binding Items}" SeparatorVisibility="None" HasUnevenRows="True" FlowLastTappedItem="{Binding SelectedLanguage}">

            <controls:FlowListView.FlowColumnsTemplates>
                <controls:FlowColumnSimpleTemplateSelector ViewType="{x:Type pages:LanguageView}"/>
                <controls:FlowColumnSimpleTemplateSelector ViewType="{x:Type pages:LanguageView}"/>
            </controls:FlowListView.FlowColumnsTemplates>
        </controls:FlowListView>
    </StackLayout>
  </ContentPage.Content>
</pages:BaseContentPage>