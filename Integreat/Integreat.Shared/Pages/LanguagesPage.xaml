<?xml version="1.0" encoding="utf-8" ?>
<pages:BaseContentPage x:Class="Integreat.Shared.Pages.LanguagesPage"
                       xmlns="http://xamarin.com/schemas/2014/forms"
                       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                       xmlns:controls="clr-namespace:DLToolkit.Forms.Controls;assembly=DLToolkit.Forms.Controls.FlowListView"
                       xmlns:pages="clr-namespace:Integreat.Shared.Pages"
                       xmlns:views="clr-namespace:Integreat.Shared.Views"
                       Title="{Binding Title}"
                       BackgroundColor="#FFC107"
                       Icon="{Binding Icon}"
                       IsBusy="{Binding IsBusy}">

    <ContentPage.Content>
        <StackLayout Orientation="Vertical">
            <controls:FlowListView FlowItemsSource="{Binding Items}"
                                   FlowLastTappedItem="{Binding SelectedLanguage}"
                                   HasUnevenRows="True"
                                   HorizontalOptions="FillAndExpand"
                                   IsPullToRefreshEnabled="True"
                                   IsRefreshing="{Binding IsBusy,
                                                          Mode=OneWay}"
                                   RefreshCommand="{Binding ForceRefreshLanguagesCommand}"
                                   SeparatorVisibility="None"
                                   VerticalOptions="FillAndExpand">

                <controls:FlowListView.FlowColumnsTemplates>
                    <controls:FlowColumnSimpleTemplateSelector ViewType="{x:Type views:LanguageView}" />
                    <controls:FlowColumnSimpleTemplateSelector ViewType="{x:Type views:LanguageView}" />
                </controls:FlowListView.FlowColumnsTemplates>
            </controls:FlowListView>
        </StackLayout>
    </ContentPage.Content>
</pages:BaseContentPage>