<?xml version="1.0" encoding="utf-8" ?>
<pages:BaseContentPage
    x:Class="Integreat.Shared.Pages.Redesign.Main.MainSingleItemDetailPage"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:converters="clr-namespace:Integreat.Shared.Converters"
    xmlns:pages="clr-namespace:Integreat.Shared.Pages"
    Title="{Binding PageToShow.Title}" BackgroundColor="White">
    <ContentPage.Resources>
        <ResourceDictionary>
            <converters:HtmlSourceConverter x:Key="HtmlSourceConverter" />
        </ResourceDictionary>
    </ContentPage.Resources>
    <StackLayout Orientation="Vertical">
        <Label IsVisible="False" x:Name="HackyBindedKey" Text="{Binding IsHtmlRawView}" />
        <WebView
            Margin="8,0,8,0" 
            HorizontalOptions="FillAndExpand"
            Source="{Binding PageToShow.Content, Converter={StaticResource HtmlSourceConverter}, ConverterParameter={x:Reference Name=HackyBindedKey}}"
            VerticalOptions="FillAndExpand" Navigating="WebView_OnNavigating">
        </WebView>
    </StackLayout>
</pages:BaseContentPage>