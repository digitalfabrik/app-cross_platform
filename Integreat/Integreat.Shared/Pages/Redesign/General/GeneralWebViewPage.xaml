<?xml version="1.0" encoding="utf-8" ?>
<pages:BaseContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:pages="clr-namespace:Integreat.Shared.Pages"
             xmlns:converters="clr-namespace:Integreat.Shared.Converters"
             x:Class="Integreat.Shared.Pages.Redesign.General.GeneralWebViewPage" Title="{Binding Source}">
    <ContentPage.Resources>
        <ResourceDictionary>
            <converters:HtmlSourceConverter x:Key="HtmlSourceConverter" />
        </ResourceDictionary>
    </ContentPage.Resources>
    <Grid>
        <WebView IsVisible="{Binding SourceIsNotHtml}" Source="{Binding Source}"/>
        <WebView IsVisible="{Binding SourceIsHtml}" Source="{Binding Source, Converter={StaticResource HtmlSourceConverter}}"/>
    </Grid>
    
</pages:BaseContentPage>