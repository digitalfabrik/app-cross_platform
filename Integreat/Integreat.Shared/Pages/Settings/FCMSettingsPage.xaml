<?xml version="1.0" encoding="utf-8" ?>
<pages:BaseContentPage xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:pages="clr-namespace:Integreat.Shared.Pages"
    x:Class="Integreat.Shared.Pages.Settings.FCMSettingsPage"
    Title="{Binding Title}" BackgroundColor="{StaticResource BackgroundColor}">
    <ContentPage.Content>
        <StackLayout>
            <Entry Placeholder="Topic" Text="{Binding TopicText}" ></Entry>
            <Button Text="Subscribe" Command="{Binding AddTopicCommand}" ></Button>
            <Button Text="Unsubscribe All" Command="{Binding UnsubscribeAllCommand}"></Button>
            <Label Text="{Binding Topics}"></Label>
        </StackLayout>
    </ContentPage.Content>
</pages:BaseContentPage>