<?xml version="1.0" encoding="utf-8" ?>
<pages:BaseContentPage x:Class="Integreat.Shared.Pages.DetailPage"
                       xmlns="http://xamarin.com/schemas/2014/forms"
                       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                       xmlns:converters="clr-namespace:Integreat.Shared.Converters"
                       xmlns:pages="clr-namespace:Integreat.Shared.Pages"
                       Title="{Binding Title}">
    <ContentPage.Resources>
        <ResourceDictionary>
            <converters:HtmlSourceConverter x:Key="HtmlSourceConverter" />
        </ResourceDictionary>
    </ContentPage.Resources>
    <ContentPage.ToolbarItems>

        <ToolbarItem Command="{Binding ChangeLocalLanguageCommand}" CommandParameter="{Binding}" Order="Primary" Priority="0" Icon="Globe.png"/>
    </ContentPage.ToolbarItems>
    <StackLayout>
        <WebView HorizontalOptions="FillAndExpand"
                 Source="{Binding Content,
                                  Converter={StaticResource HtmlSourceConverter}}"
                 VerticalOptions="FillAndExpand" />
    </StackLayout>
</pages:BaseContentPage>