<?xml version="1.0" encoding="utf-8" ?>
<MasterDetailPage  xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:pages="clr-namespace:Integreat.Shared.Pages"
             x:Class="Integreat.Shared.Pages.MainPage" 
            Title = "Title" IsPresented="{Binding NavigationViewModel.IsPresented, Mode=TwoWay}">
  <MasterDetailPage.Detail>
    <NavigationPage BarBackgroundColor="#FFD500" BarTextColor="Black">
      <x:Arguments>
        <pages:TabPage Title="Integreat" BindingContext="{Binding TabViewModel}"/>
      </x:Arguments>
    </NavigationPage>
  </MasterDetailPage.Detail>
  <MasterDetailPage.Master >
     <pages:NavigationDrawerPage Title = "☰" BindingContext="{Binding NavigationViewModel}"/>
  </MasterDetailPage.Master>
</MasterDetailPage >