init:
- appveyor version
- ping api.nuget.org

version: 2.0.{build}

configuration: AppStore

#shallow_clone: true
platform: Any CPU

# restore packages
before_build:
  - nuget restore

build:
  verbosity: minimal
